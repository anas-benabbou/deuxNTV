import React from 'react';
import { View, Text } from 'react-native';

const MyListScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>My List Screen</Text>
      {/* Display user's list of movies */}
    </View>
  );
};

export default MyListScreen;
